用户体验：让用户用的爽

要有大量的特效：合理性，性能上（不能运行一会就变卡了）

用新的技术：H5、CSS3

开发过程中和后期维护上：多人协作，功能扩展

一站式开发：单页面应用，利用Hash，让用户能够体验到类似Flash网站的感觉
Seajs库：
jQuery库：
Html5 + CSS3 高版本的兼容，低版本老官网兼容

用CSS3做运动比JS定时器做性能更高

CSS3运动库 + startMove()

jQuery当中的运动是时间版运动，而startMove()是速度版的
jQuery的运动中，有运动完成后的回调函数，但是是没有边运动边调用的回调函数，startMove()当中就可以扩展

引入了jCanvasScript的库，Canvas中的jQuery

用到的库：Seajs、jQuery、jCanvasScript、move.js
